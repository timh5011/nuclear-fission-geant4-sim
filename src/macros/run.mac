# run.mac — headless batch-mode default for the nuclear-fission sim.
# Invoke:  ./nuclear_fission run.mac
# After Phase B lands, this writes data/<UTC>/hits.csv + events.csv at repo root.

/run/initialize
/run/printProgress 100
/run/beamOn 50000
